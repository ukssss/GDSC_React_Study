const main = {
    padding: "40px 30px",
    textAlign: "center",    
}

const input = {
    backgroundColor: "transparent",
    fontSize: "19px",
    color: "black",
    textAlign: "center",
    border: "none",
    borderBottom: "1px solid #8C82FC",
    transition: "all 0.4s",
    marginBottom:"20px",
    marginTop:"-50px",
}


const inputButton = {
    color: "white",
    outline: "none",
    fontSize: "18px",
    fontWeight: "300",
    backgroundColor: "#8C82FC",
}

const hintButton = {
    backgroundColor: "red",
    color: "white",
}

const tryStyle = {
    lineHeight: 1.5,
    color: "red",
}

const style = {
    inputButton: inputButton,
    main: main,
    tryStyle: tryStyle,
    input: input,
    hintButton: hintButton
}

export default style;